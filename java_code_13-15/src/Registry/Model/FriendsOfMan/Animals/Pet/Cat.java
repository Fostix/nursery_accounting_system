package Registry.Model.FriendsOfMan.Animals.Pet;

public class Cat extends Pet {
    public Cat(int number, int id, String dateOfBirth, String name) {
        super(number, id, dateOfBirth, name);
    }
}
